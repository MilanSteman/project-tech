{% extends  "layouts/layout.njk" %}
{% block  content %}
  <h1>{{message}}</h1>

  {% if authorised %}
    <p>{{description}}</p>

    {% else %}
      <p>{% include "partials/error.njk" %}</p>
  {% endif %}
{% endblock %}